      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f trm_cloud/requirements.txt ]; then pip install -r trm_cloud/requirements.txt; fi
