name: TRM Cloud Automation

on:
  workflow_dispatch:
  schedule:
    - cron: '0 17 * * *' # Türkiye saati 20:00

jobs:
  run-trm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run TRM script
        run: python trm_cloud/main.py



          path: trm_reports


