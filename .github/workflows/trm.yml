name: TRM Cloud Automation
on:
  workflow_dispatch:
  schedule:
    - cron: '0 17 * * *'   # Türkiye 20:00

jobs:
  run-trm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: |
          mkdir -p trm_reports
          python -V
          python trm_cloud/main.py | tee trm_reports/run.log

      - uses: actions/upload-artifact@v4
        with:
          name: trm-reports
          path: trm_reports


          name: trm-reports
          path: trm_reports

